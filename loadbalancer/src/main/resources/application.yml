spring:
  application:
    name: ribbon-client
server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ # Eureka 服务地址
  instance:
    prefer-ip-address: true